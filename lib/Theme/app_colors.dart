import 'package:flutter/material.dart';

class SAppColors {
  // Primary Colour (Confirmation buttons - 600 Blue)
  static const Color primaryBlue = Color(0xFF02386E);

  // Secondary Colour (Darker 700 Blue)
  static const Color secondaryDarkBlue = Color(0xFF00264D);

  // Light Blue Buttons (Accent/Tertiary)
  static const Color lightBlue = Color(0xFF3388E6);

  // Outline / Unselected Buttons (300 Gray)
  static const Color outlineGray = Color(0xFF9DA4AE);

  // Main Text Colour (800 Gray)
  static const Color textGray = Color(0xFF1F2A37);

  // Description Text Colour (400 Gray)
  static const Color descriptionTextGray = Color(0xFF9DA4AE);

  // Page Background
  static const Color background = Color(0xFFF5F5F5);

  // Dark Page Backgroundd
  static const Color darkBackground = Color(0xFF3E3E42);

  // Error Colour (600 Red)
  static const Color error = Color(0xFFD92D20);

  // White (Useful helper for text on dark buttons)
  static const Color white = Colors.white;

  // Transparent Colour
  static const Color transparent = Colors.transparent;
}